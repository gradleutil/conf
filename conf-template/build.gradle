plugins {
    id 'groovy'
}
buildDir('../build/conf-template')

dependencies {
    compileOnly localGroovy()
}

sourceSets {
    main {
        java {
            srcDirs = [] // don't compile Java code twice
        }
        groovy {
            srcDirs = [ 'src/']
        }
    }
}