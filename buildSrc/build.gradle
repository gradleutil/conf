plugins {
    id 'groovy'
}

dependencies {
    compileOnly localGroovy()
}

sourceSets {
    main {
        java {
            srcDirs = [] // don't compile Java code twice
        }
        groovy {
            srcDirs = [ 'src/']
        }
    }
}